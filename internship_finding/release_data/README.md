# release_data

该目录用于存放可上传的数据快照（从 `official_jobs_raw.csv` 与 `outputs/` 复制）。

当前快照文件命名约定：

- `official_jobs_raw_latest.csv`
- `data_quality_report_latest.csv`
- `dashboard_all_summary_latest.csv`
- `internship_target_jobs_latest.csv`
- `broken_links_report_latest.csv`
